#include "gdt.ika"
#include "idt.ika"
#include "irq.ika"

#include "console/vga_text.ika"

#include "print.ika"

fn kmain(magic: u32, mbinfo: u32) void {
    gdt_init();
    idt_init();
    irq_init();
    asm("sti");
    vga_init();


    "takos is running! %s\n", "WAH!";

    while (true) {
        asm("hlt");
    }
}
