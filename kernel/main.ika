#include "gdt.ika"
#include "idt.ika"
#include "irq.ika"

#include "console.ika"

#include "print.ika"

fn kmain(magic: u32, mbinfo: u32) void {
    gdt_init();
    idt_init();
    irq_init();
    asm("sti");

    console_init();

    "takos is running! %s\n", "WAH!";

    while (true) {
        asm("hlt");
    }
}
